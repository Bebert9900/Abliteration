# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Caches outils
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Environnements virtuels / uv
.venv/
venv/
env/
.env

# Modèles & artefacts lourds (ne jamais committer de poids)
# `artifacts/` = modèles abliteré produits (out-3b-abl, out-3b-npb, …) — local seulement.
out/
out*/
outputs/
artifacts/
checkpoints/
*.safetensors
*.gguf
*.bin
*.pt
*.pth
*.ckpt

# Caches HuggingFace / datasets locaux
.cache/
hf_cache/

# Générations brutes d'éval = peuvent contenir du contenu harmful (sorties du modèle
# abliteré). Ne JAMAIS publier sur un repo public. Les rapports agrégés (scores/labels) OK.
rejudge_generations.json
*_generations.json

# Divers
.DS_Store
.idea/
.vscode/
*.log
*.swp
*.tmp
*.bak
.smoke.txt
files(*)/

# Fichiers de contexte/développement PRIVÉS — ne JAMAIS publier sur le repo distant
# (instructions projet, base de connaissances, skills, specs internes).
# Purgés de tout l'historique git (git-filter-repo) ; restent présents en local.
.claude/
docs/
CLAUDE.md
ABLITERATION_KNOWLEDGE_BASE.md
